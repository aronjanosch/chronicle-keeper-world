# Screenshots

App screenshots used by the top-level [`README.md`](../../README.md). Filenames are referenced
directly there — replace a file (keep the name) to update the image on the landing page.
PNG/JPEG, ≈1200px wide.
