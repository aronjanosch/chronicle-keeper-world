# Screenshots

Drop the README's screenshots here. The top-level `README.md` references these exact filenames:

| File | What to capture |
| --- | --- |
| `library.png` | The campaign library (the seeded **Example** campaign card is fine). |
| `session.png` | A session screen showing tracks / transcript. |
| `summary.png` | A finished AI summary (the sample session works well). |

Keep them reasonably sized (≈1200px wide, PNG). Until real images are added, the README links
will show as broken-image placeholders.
